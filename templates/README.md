# DOCX templates

Optional files: `manufacturing_act.docx`, `test_protocol.docx`, `test_act.docx`.
Supported paragraph placeholders: `{{title}}`, `{{document_id}}`, `{{generated_text}}`.
If a template is absent, the generator creates a minimal document programmatically.

The repository includes editable starter templates. Keep a backup before changing them.
